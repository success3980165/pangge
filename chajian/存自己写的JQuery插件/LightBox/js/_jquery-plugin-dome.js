;(function($) {
	// 声明一个构造函数LightBox
	var LightBox = function() {

	};
	// 在LightBox的原型上储存所用的方法
	LightBox.prototype = {

	};
	// 将这个LightBox构造函数注册到window对象上面
	window['LightBox'] = LightBox;
})(jQuery);